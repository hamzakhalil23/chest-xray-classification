# Model artefacts

Trained model files are not included. Run the training notebook and copy its exported `efficientnet_b0.pt` and `config.json` here. The app expects these names. Verify the class mapping and use only trusted checkpoints. Record SHA-256 and dependency versions alongside any published download. No model download link is currently verified.
